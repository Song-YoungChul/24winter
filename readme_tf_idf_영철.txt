TF-IDF
- Term Frequency - Inverse Document Frequency
- 단어 수의 빈도만이 아닌 문서 내 단어 출현 빈도를 활용해 문서 집단 내에서 각 단어의 중요도를 계산하는 방법

계산 방법
- TF-IDF는 TF와 IDF의 곱으로 나타남
- TF는 특정 문서 a에서 특정 단어의 등장 횟수를 나타냄
> TF(d, t) = (문서 d 내의 t의 단어가 몇 개 등장했는가)
- IDF는 특정 문서 집단 내에서 특정 단어의 등장 문서 수의 비율을 반비례하여 log를 씌운 값을 의미함
> DF(t) = (문서 집단 내에서 t의 단어가 몇 개의 문서에서 등장했는가)
> IDF(t) = log(D/1+IDF(t)), D = (총 문서 갯수)
- 이 두개를 곱해 TF-IDF 값을 구하게 됨
> TF-IDF = TF(d, t) * IDF(t)

- TF-IDF의 의미는 범용적으로 많이 쓰이는 단어는 낮은 값을, 특정 문서에서 주제로 사용될 수 있는 단어들은 높은 값을 나타내게 됨

-----------------------------------------------------------
코드 사용 방법

1. 본 코드는 위즈 도메인에서 불러온 특허 데이터를 바로 활용 가능하도록 설계됨
- 위즈 도메인에서 수집한 데이터(.csv 기준)에서 5행까지의 데이터를 삭제 후 저장함
2. pd.read_csv("파일 이름.csv")를 통해 파일을 불러오며, #통합버젼 5번째 줄 내 파일 이름을 수정하면 사용 가능함
- 이 외에도 pandas dataframe 형태로 하나의 열의 텍스트로 구성된 내용들을 넣으면 띄어쓰기 기준으로 단어가 분리되고 분리된 단어들을 대상으로 tf-idf가 수행됨
3. pandas dataframe 형태의 데이터가 함수로 들어가야 하며, 하나의 열의 텍스트로 구성된 내용이 포함되어 있어야 함
4. total_TFIDF 함수를 이용하며 3에서 준비한 데이터를 변수로 입력하면 각 행은 하나의 특허를, 각 열은 하나의 단어를 나타내는 tf-idf 결과 값이 도출됨